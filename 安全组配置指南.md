# 云电脑安全组配置指南

## 问题原因

云电脑的公网 IP：**115.190.93.94**
云电脑的内网 IP：**9.129.131.227**

外部无法访问是因为云服务商的**安全组（防火墙）没有开放端口**。

## 需要开放的端口

- **TCP 5000**：前端 H5 页面
- **TCP 3000**：后端 API 服务

## 配置步骤

### 方式一：阿里云 ECS

1. 登录 [阿里云控制台](https://ecs.console.aliyun.com/)
2. 找到您的云服务器实例
3. 点击"安全组" → "配置规则"
4. 点击"添加安全组规则"
5. 添加以下入站规则：

**规则 1（前端）**：
- 规则方向：入方向
- 授权策略：允许
- 协议类型：自定义 TCP
- 端口范围：5000/5000
- 授权对象：0.0.0.0/0
- 优先级：1

**规则 2（后端）**：
- 规则方向：入方向
- 授权策略：允许
- 协议类型：自定义 TCP
- 端口范围：3000/3000
- 授权对象：0.0.0.0/0
- 优先级：1

### 方式二：腾讯云 CVM

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/cvm)
2. 找到您的云服务器实例
3. 点击"安全组" → "修改规则"
4. 点击"添加规则"
5. 添加以下入站规则：

**规则 1（前端）**：
- 类型：自定义
- 来源：0.0.0.0/0
- 协议端口：TCP:5000
- 策略：允许

**规则 2（后端）**：
- 类型：自定义
- 来源：0.0.0.0/0
- 协议端口：TCP:3000
- 策略：允许

### 方式三：华为云 ECS

1. 登录 [华为云控制台](https://console.huaweicloud.com/ecm/)
2. 找到您的弹性云服务器
3. 点击"安全组" → "配置规则"
4. 点击"添加规则"
5. 添加以下入站规则：

**规则 1（前端）**：
- 协议端口：TCP 5000
- 源地址：0.0.0.0/0
- 策略：允许

**规则 2（后端）**：
- 协议端口：TCP 3000
- 源地址：0.0.0.0/0
- 策略：允许

### 方式四：Coze 云电脑

如果使用的是 Coze 提供的云电脑，可能需要：

1. 在云电脑管理控制台找到"网络配置"或"端口映射"
2. 添加端口转发规则：
   - 外部端口 5000 → 内部端口 5000
   - 外部端口 3000 → 内部端口 3000

## 配置完成后

等待 1-2 分钟让安全组规则生效，然后访问：

**系统地址**：
```
http://115.190.93.94:5000/
```

## 验证配置

配置完成后，可以使用以下命令验证端口是否开放：

```bash
curl -I http://115.190.93.94:5000/
```

如果返回 `HTTP/1.1 200 OK`，说明配置成功。

## 安全提示

⚠️ **重要安全建议**：

当前配置 `0.0.0.0/0` 表示允许任何 IP 访问，这是为了方便测试。生产环境建议：

1. 限制源 IP 范围（只允许特定 IP 访问）
2. 使用 HTTPS 加密传输
3. 配置 Web 应用防火墙（WAF）
4. 定期更新系统和应用补丁

## 临时解决方案：端口转发

如果您无法配置安全组，可以使用 SSH 端口转发：

### 在本地电脑执行：

```bash
ssh -L 5000:localhost:5000 -L 3000:localhost:3000 用户名@115.190.93.94
```

然后在本地浏览器访问：
```
http://localhost:5000/
```

## 联系支持

如果以上方法都无法解决，请联系您的云服务商技术支持，询问如何开放端口 5000 和 3000。
