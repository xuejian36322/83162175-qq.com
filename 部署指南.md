# 陕西叁恒企业业务管理系统 - 部署指南

## 📦 文件说明

系统已成功构建，生产文件已打包为：
```
sanheng-business-web.tar.gz (175KB)
```

这个文件包含完整的前端应用（H5 版本），可以直接部署到任何支持静态文件托管的服务器。

---

## 🚀 快速部署方案（推荐）

### 方案 1：阿里云 OSS（静态网站托管）⭐

1. **登录阿里云控制台**
   - 访问：https://oss.console.aliyun.com/
   - 创建存储空间（Bucket）

2. **配置 Bucket**
   - 存储类型：标准存储
   - 读写权限：公共读
   - 开启"静态网站托管"

3. **上传文件**
   - 解压 `sanheng-business-web.tar.gz`
   - 将 `dist-web` 目录下的所有文件上传到 OSS
   - 上传后访问 OSS 提供的域名即可

**费用**：几乎免费（存储和流量费用极低）

---

### 方案 2：腾讯云 COS（静态网站托管）⭐

1. **登录腾讯云控制台**
   - 访问：https://console.cloud.tencent.com/cos5
   - 创建存储桶

2. **配置存储桶**
   - 访问权限：公共读
   - 开启"静态网站"

3. **上传文件**
   - 解压 `sanheng-business-web.tar.gz`
   - 上传 `dist-web` 目录下的所有文件
   - 访问静态网站域名

**费用**：几乎免费（存储和流量费用极低）

---

### 方案 3：免费托管平台（Vercel）⭐⭐⭐

**前提**：需要将代码上传到 GitHub

1. **推送到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/你的用户名/你的仓库名.git
   git push -u origin main
   ```

2. **部署到 Vercel**
   - 访问：https://vercel.com/
   - 点击"New Project"
   - 选择你的 GitHub 仓库
   - 自动部署完成

3. **获取访问地址**
   - Vercel 会自动分配一个 .vercel.app 域名
   - 可以自定义域名

**费用**：完全免费

---

### 方案 4：免费托管平台（Netlify）⭐⭐⭐

1. **推送到 GitHub**（同上）

2. **部署到 Netlify**
   - 访问：https://app.netlify.com/
   - 点击"Add new site" → "Import an existing project"
   - 选择你的 GitHub 仓库
   - 设置：
     - Build command: `pnpm build:web`
     - Publish directory: `dist-web`
   - 点击"Deploy site"

**费用**：完全免费

---

### 方案 5：自己的云服务器

如果您有自己的云服务器（阿里云、腾讯云、华为云等）：

1. **安装 Nginx**
   ```bash
   # Ubuntu/Debian
   sudo apt update
   sudo apt install nginx -y

   # CentOS/RHEL
   sudo yum install nginx -y
   ```

2. **上传文件**
   ```bash
   # 上传 sanheng-business-web.tar.gz 到服务器
   # 解压到 /var/www/html/
   tar -xzf sanheng-business-web.tar.gz -C /var/www/html/
   mv /var/www/html/dist-web/* /var/www/html/
   ```

3. **配置 Nginx**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;  # 替换为您的域名或服务器 IP

       location / {
           root /var/www/html;
           index index.html;
           try_files $uri $uri/ /index.html;
       }

       # 配置后端 API 代理
       location /api {
           proxy_pass http://your-backend-server:3000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

4. **重启 Nginx**
   ```bash
   sudo systemctl restart nginx
   ```

---

## 🔧 后端部署

前端部署后，还需要部署后端服务。有两种方案：

### 方案 A：使用 Supabase（推荐）

系统已经配置使用 Supabase 作为数据库，只需要：

1. **保持后端服务运行**
   - 可以在扣子空间的云电脑上运行后端
   - 或者部署到其他服务器

2. **配置前端连接**
   - 修改 `.env.local` 文件中的 `PROJECT_DOMAIN`
   - 设置为您的后端服务器地址

### 方案 B：部署到云服务器

1. **安装 Node.js 和 pnpm**
   ```bash
   # 安装 Node.js
   curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
   sudo yum install -y nodejs

   # 安装 pnpm
   npm install -g pnpm
   ```

2. **上传项目文件**
   ```bash
   # 上传整个项目到服务器
   # 安装依赖
   cd /workspace/projects
   pnpm install

   # 构建后端
   pnpm build:server

   # 启动后端（使用 PM2 保持运行）
   npm install -g pm2
   pm2 start npm --name "backend" -- run "start:prod"
   ```

3. **配置环境变量**
   - 在服务器上创建 `.env` 文件
   - 配置数据库连接信息

---

## 📝 环境变量配置

前端需要配置后端 API 地址：

### 开发环境
```bash
# .env.local
PROJECT_DOMAIN=http://your-backend-server:3000
```

### 生产环境
```bash
# 构建前设置
export PROJECT_DOMAIN=https://your-api-domain.com
pnpm build:web
```

---

## 🌐 访问系统

部署完成后，通过以下地址访问：

**前端地址**：您部署的域名或 IP 地址
- 例如：https://your-domain.com 或 http://your-server-ip

**后端 API**：`http://your-backend-server:3000/api`

---

## 📞 常见问题

### Q1: 部署后页面空白？
A: 检查是否正确配置了后端 API 地址，打开浏览器控制台查看错误信息。

### Q2: 无法调用 API？
A:
1. 确认后端服务正在运行
2. 检查防火墙是否开放了 3000 端口
3. 检查 CORS 配置

### Q3: 如何配置 HTTPS？
A:
- 使用免费的 Let's Encrypt 证书
- 在 Nginx 配置中添加 SSL 证书

### Q4: 如何更新系统？
A:
1. 修改代码
2. 重新构建：`pnpm build:web`
3. 重新上传文件

---

## 💡 推荐方案

**最简单、最省心的方案**：

1. **前端**：部署到 Vercel（完全免费，自动 HTTPS，支持自定义域名）
2. **后端**：使用现有的 Supabase 数据库，后端服务可以运行在任何云服务器

**总成本**：几乎为 0（免费托管 + Supabase 免费套餐）

---

## 📞 技术支持

如果遇到问题，可以：
1. 查看浏览器控制台的错误信息
2. 检查后端服务日志
3. 确认环境变量配置正确

---

## 🎉 开始使用

部署完成后，访问您的网站：

1. 点击"微信登录"
2. 输入任意测试代码（例如：`test_code_18700999611`）
3. 系统自动创建超级管理员账户
4. 开始使用系统功能！

祝您使用愉快！
