# 用户审批管理系统配置完成

## ✅ 完成时间

**完成时间**: 2025-01-17

---

## 🎯 功能实现

### 1. 数据库表结构更新

**users 表新增字段**：

| 字段名 | 类型 | 说明 | 默认值 |
|--------|------|------|--------|
| approval_status | varchar(20) | 审批状态 | 'pending' |
| approved_by | varchar(36) | 审批人ID | null |
| approved_at | timestamp | 审批时间 | null |
| approval_remarks | text | 审批备注 | null |

**审批状态**：
- `pending` - 待审批
- `approved` - 已通过
- `rejected` - 已拒绝

### 2. 超级管理员账户

已创建超级管理员账户：

| 字段 | 值 |
|------|-----|
| 姓名 | 薛健（超级管理员） |
| 手机号 | 18700999611 |
| 角色 | super_admin |
| 审批状态 | approved（已自动审批） |
| 权限 | 系统最高权限，可以审批所有用户 |

### 3. 用户注册审批流程

**新用户注册流程**：

1. **用户微信登录**
   - 使用微信授权获取 openId
   - 提交注册信息（姓名、手机号等）
   - 系统自动创建账户，状态为 `pending`

2. **等待审批**
   - 用户登录后提示："您的注册申请已提交，请联系管理员（18700999611）审批后才能使用系统"
   - 无法访问任何功能页面

3. **管理员审批**
   - 超级管理员登录系统
   - 进入「设置」→「用户审批管理」
   - 查看待审批用户列表
   - 选择「通过」或「拒绝」

4. **审批结果**
   - **通过**: 用户可以正常登录使用系统
   - **拒绝**: 用户无法登录，需要重新申请

---

## 🔧 后端接口

### 用户注册

```
POST /api/users/register
```

**请求参数**：
```json
{
  "openId": "wx_openid_xxx",
  "name": "张三",
  "phone": "13800138000",
  "avatarUrl": "https://...",
  "company": "sanheng_jiliang"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "注册成功，等待管理员审批",
  "data": { ... }
}
```

### 获取待审批用户列表

```
GET /api/users/pending
```

**权限**：仅超级管理员

**响应**：
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": [
    {
      "id": "user_id",
      "name": "张三",
      "phone": "13800138000",
      "company": "sanheng_jiliang",
      "approval_status": "pending",
      "created_at": "2025-01-17T10:00:00Z"
    }
  ]
}
```

### 审批用户

```
PUT /api/users/:id/approve
```

**权限**：仅超级管理员

**请求参数**：
```json
{
  "approverId": "super_admin_id",
  "approved": true,
  "remarks": "审批通过"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "已通过",
  "data": { ... }
}
```

---

## 📱 前端页面

### 1. 用户审批页面

**路径**: `pages/user-approval/index`

**功能**：
- 显示待审批用户列表
- 查看用户详细信息
- 一键通过/拒绝
- 显示审批状态和时间

**权限控制**：
- 只有超级管理员可以访问
- 其他用户访问会自动跳转

### 2. 登录页面修改

**修改内容**：
- 登录成功后检查 `approval_status`
- `pending` 状态：显示等待审批提示，返回登录页
- `rejected` 状态：显示已被拒绝提示
- `approved` 状态：正常登录

### 3. 设置页面新增

**超级管理员专属入口**：
- 添加「用户审批管理」按钮
- 显示红色盾牌图标，表示管理员权限
- 点击跳转到审批页面

---

## 🔒 权限控制

### 超级管理员权限

**唯一账号**: 18700999611（薛健）

**可以访问的功能**：
- ✅ 用户审批管理
- ✅ 用户管理（增删改查）
- ✅ 角色管理（分配角色）
- ✅ 订单管理（所有订单）
- ✅ 费用申请审批
- ✅ 开票申请审批
- ✅ 数据统计查看
- ✅ 系统设置

### 普通用户权限

**未审批用户**：
- ❌ 无法登录
- ❌ 无法访问任何功能

**已审批用户**：
- ✅ 登录系统
- ✅ 查看订单列表
- ✅ 创建订单（有权限的角色）
- ✅ 提交费用申请
- ✅ 提交开票申请
- ❌ 无法审批其他用户
- ❌ 无法修改用户角色

---

## 📝 使用流程

### 新用户注册流程

```
1. 新用户打开小程序
   ↓
2. 点击「微信登录」
   ↓
3. 授权获取微信信息
   ↓
4. 系统自动创建账户（状态：pending）
   ↓
5. 提示等待审批
   ↓
6. 联系管理员 18700999611
   ↓
7. 管理员审批（通过/拒绝）
   ↓
8. 通过后可正常登录使用
```

### 管理员审批流程

```
1. 管理员使用 18700999611 登录
   ↓
2. 进入「我的」→「系统设置」
   ↓
3. 点击「用户审批管理」
   ↓
4. 查看待审批用户列表
   ↓
5. 点击「通过」或「拒绝」
   ↓
6. 审批完成，用户状态更新
```

---

## 🎨 界面预览

### 用户审批页面

- **顶部提示**: 显示超级管理员权限说明
- **用户卡片**:
  - 用户头像（首字母）
  - 姓名、手机号
  - 所属公司
  - 注册时间
  - 审批状态标签
- **操作按钮**:
  - 绿色「通过」按钮
  - 红色「拒绝」按钮

### 登录提示

**待审批用户**:
```
等待审批

您的注册申请已提交，请联系管理员（18700999611）
审批后才能使用系统
```

**已拒绝用户**:
```
注册已被拒绝
```

---

## ⚠️ 重要说明

### 微信验证通知

**当前状态**: 系统已支持用户注册审批流程，但**暂未实现微信消息通知**。

**原因**:
- 需要配置微信服务号模板消息
- 需要申请微信服务号（非订阅号）
- 需要配置消息推送接口

**临时方案**:
- 新用户登录后看到审批提示
- 提示用户主动联系管理员 18700999611
- 管理员可以查看所有待审批用户

### 如需实现微信通知

需要完成以下步骤：

1. **申请微信服务号**
   - 访问 https://mp.weixin.qq.com
   - 申请服务号（认证）

2. **配置模板消息**
   - 创建审批通知模板
   - 提交审核

3. **集成消息推送**
   - 在审批通过后调用微信接口
   - 发送模板消息给用户

**成本**: 服务号每年 300 元认证费

---

## 🚀 下一步建议

1. **测试审批流程**
   - 使用新手机号注册测试账号
   - 验证审批功能是否正常

2. **完善微信通知**（可选）
   - 申请微信服务号
   - 配置模板消息
   - 实现自动通知

3. **管理员培训**
   - 教会管理员使用审批功能
   - 建立审批流程规范

---

## 📞 联系方式

**超级管理员**: 薛健
**手机号**: 18700999611
**密码**: xuejian36322

---

## ✅ 配置完成！

用户审批管理系统已完全配置完成并可以使用！

新用户现在可以通过微信登录注册，等待超级管理员审批后才能使用系统功能。
