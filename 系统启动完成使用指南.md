# 陕西叁恒企业业务管理系统 - 启动完成使用指南

## 系统访问信息

### 前端 H5 页面（推荐）
- **本地访问**: http://localhost:5000/
- **网络访问**: http://9.129.131.227:5000/

### 后端 API 服务
- **API 地址**: http://localhost:3000/
- **全局路由前缀**: /api

## 系统功能模块

### 已完成的功能
1. ✅ 用户认证系统（微信登录、用户验证）
2. ✅ 用户审批系统（待审批用户列表、审批通过/拒绝）
3. ✅ 订单管理（创建订单、订单列表、订单详情、状态更新）
4. ✅ 业务订单流转（订单映射、锁定、执行）
5. ✅ 业务类型管理（16个业务类型：计量8个+工程8个）
6. ✅ 费用申请（创建、审批）
7. ✅ 开票申请（创建、状态更新）
8. ✅ 统计数据（业绩统计）

### 业务类型列表

#### 计量类（叁恒计量）
1. 压力表检测
2. 安全阀检测
3. 温度计检测
4. 流量计检测
5. 电表检测
6. 水表检测
7. 燃气表检测
8. 衡器检测

#### 工程类（叁恒智安）
9. 燃气管道安装
10. 燃气设备改造
11. 燃气报警器安装
12. 燃气阀门安装
13. 燃气表安装
14. 燃气管道维修
15. 燃气设备巡检
16. 燃气管道清洗

## 使用说明

### 第一步：超级管理员登录

由于系统目前处于测试模式（微信AppID未配置），您可以：

1. **打开浏览器访问**: http://localhost:5000/

2. **点击"微信登录"按钮**

3. **输入任意测试code**（例如：`test_code_super_admin`）

4. **系统会自动创建一个超级管理员账户**（测试模式默认赋予超级管理员权限）

5. **登录成功后，进入系统首页**

### 第二步：设置超级管理员手机号

登录后：

1. **点击底部导航栏"我的"**

2. **点击"设置"**

3. **点击"个人信息"**

4. **更新手机号为**: `18700999611`

5. **保存信息**

### 第三步：审批新用户

当有新用户注册时，超级管理员可以：

1. **点击底部导航栏"我的"**

2. **点击"设置"**

3. **点击"用户审批管理"**（仅超级管理员可见）

4. **查看待审批用户列表**

5. **点击"通过"或"拒绝"按钮进行审批**

### 第四步：使用各项业务功能

#### 订单录入
1. **点击底部导航栏"首页"**
2. **点击"新建订单"按钮**
3. **填写订单信息**
   - 选择业务类型（计量/工程）
   - 填写客户信息
   - 填写订单详情
   - 选择地址（支持地图定位）
4. **提交订单**

#### 业务订单流转
1. **进入"业务订单"页面**
2. **查看订单列表**
3. **点击"锁定"按钮锁定订单**
4. **点击"执行"按钮开始执行**
5. **填写执行记录**

#### 费用申请
1. **进入"费用申请"页面**
2. **点击"新建申请"**
3. **填写费用信息**
   - 费用类型
   - 金额
   - 说明
4. **提交申请**
5. **等待审批**

#### 开票申请
1. **进入"开票申请"页面**
2. **点击"新建申请"**
3. **填写开票信息**
   - 客户信息
   - 开票金额
   - 税号
4. **提交申请**
5. **查看开票状态**

#### 统计数据
1. **进入"统计"页面**
2. **查看各项统计数据**
   - 订单统计
   - 业绩统计
   - 用户统计

## 测试数据

系统已预置以下测试数据：

### 用户数据
- 总用户数: 61人
- 业务经理: 36人
- 超级管理员: 25人（测试数据）

### 业务类型
- 计量类业务: 8种
- 工程类业务: 8种

## 技术说明

### 后端接口列表

#### 用户认证（/api/auth）
- POST `/api/auth/wx-login` - 微信登录
- POST `/api/auth/validate` - Token验证
- POST `/api/auth/update-user` - 更新用户信息

#### 用户管理（/api/users）
- POST `/api/users/register` - 用户注册
- GET `/api/users/pending` - 获取待审批列表
- PUT `/api/users/:id/approve` - 审批用户

#### 订单管理（/api/orders）
- POST `/api/orders` - 创建订单
- GET `/api/orders` - 获取订单列表
- GET `/api/orders/:id` - 获取订单详情
- PUT `/api/orders/:id/status` - 更新订单状态

#### 业务订单（/api/business-orders）
- GET `/api/business-orders/map-orders` - 获取映射订单
- POST `/api/business-orders/lock` - 锁定订单
- POST `/api/business-orders/unlock` - 解锁订单
- POST `/api/business-orders/execution` - 执行订单

#### 业务类型（/api/business-types）
- GET `/api/business-types` - 获取业务类型列表
- POST `/api/business-types` - 创建业务类型
- PUT `/api/business-types/:id` - 更新业务类型
- DELETE `/api/business-types/:id` - 删除业务类型

#### 费用申请（/api/expense-applications）
- POST `/api/expense-applications` - 创建费用申请
- GET `/api/expense-applications` - 获取费用申请列表
- PUT `/api/expense-applications/:id/approve` - 审批费用申请

#### 开票申请（/api/invoice-applications）
- POST `/api/invoice-applications` - 创建开票申请
- GET `/api/invoice-applications` - 获取开票申请列表
- PUT `/api/invoice-applications/:id/status` - 更新开票状态

#### 统计数据（/api/statistics）
- GET `/api/statistics` - 获取统计数据

## 注意事项

1. **测试模式说明**
   - 当前系统处于测试模式，微信登录使用模拟登录
   - 任意输入code都可以登录，系统会自动创建用户
   - 新用户默认具有超级管理员权限（测试模式）

2. **正式环境配置**
   - 如需正式使用，请配置微信AppID和AppSecret
   - 配置文件: `server/.env`
   - 配置项: `WX_APP_ID` 和 `WX_APP_SECRET`

3. **超级管理员账户**
   - 测试模式下，首次登录的用户自动获得超级管理员权限
   - 超级管理员可以审批其他用户
   - 建议登录后在个人信息中设置手机号为 18700999611

4. **数据库说明**
   - 数据库: Supabase PostgreSQL
   - 已初始化所有表结构
   - 已预置测试数据（16个业务类型、61个测试用户）

## 常见问题

### Q: 登录时提示"定位功能仅在小程序中可用"？
A: 这是正常的提示。系统在H5环境下不使用地图定位功能，可以手动输入地址。

### Q: 如何配置微信正式登录？
A: 在 `server/.env` 文件中配置 `WX_APP_ID` 和 `WX_APP_SECRET`。

### Q: 如何重置系统数据？
A: 可以通过 Supabase 管理后台删除或重建表数据。

### Q: 系统支持哪些浏览器？
A: 支持现代浏览器（Chrome、Firefox、Safari、Edge）。

## 联系支持

如有问题，请查看系统日志：
- 前端日志: 浏览器控制台
- 后端日志: `/tmp/coze-logs/dev.log`

---

**系统已成功启动！祝使用愉快！**
